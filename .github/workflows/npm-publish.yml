name: npm publish

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Use Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      # Trusted publishing (OIDC) requires npm >= 11.5.1; Node 22 ships npm 10.x.
      - name: Upgrade npm
        run: |
          npm install -g npm@latest
          npm --version

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: node tests/run-unit-tests.js

      - name: Determine versions
        id: versions
        run: |
          echo "PKG_NAME=$(node -p "require('./package.json').name")" >> $GITHUB_ENV
          echo "LOCAL_VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_ENV
          REMOTE_VERSION=$(npm view $(node -p "require('./package.json').name") version || echo 0.0.0)
          echo "REMOTE_VERSION=$REMOTE_VERSION" >> $GITHUB_ENV
          if [ "$REMOTE_VERSION" = "$LOCAL_VERSION" ]; then
            echo "publish=false" >> $GITHUB_OUTPUT
          else
            echo "publish=true" >> $GITHUB_OUTPUT
          fi

      # Publishes via trusted publishing: npm exchanges this workflow's OIDC
      # identity (`id-token: write`) for a short-lived registry credential.
      # danylaksono/screengrid + npm-publish.yml are registered as a trusted
      # publisher on the package's npm settings. There is deliberately no
      # NODE_AUTH_TOKEN fallback -- a long-lived token would reintroduce the
      # 2FA-bypass path npm is deprecating, and would let an OIDC
      # misconfiguration fail as a confusing auth error instead of loudly.
      - name: Publish to npm
        if: steps.versions.outputs.publish == 'true'
        run: npm publish --access public --provenance

      - name: Summary
        run: |
          echo "package: $PKG_NAME" >> $GITHUB_STEP_SUMMARY
          echo "local: $LOCAL_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "remote: $REMOTE_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "published: ${{ steps.versions.outputs.publish }}" >> $GITHUB_STEP_SUMMARY


