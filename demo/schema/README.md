# Moved

The Screengrid grammar schemas are now part of the library:
[`src/grammar/schemas/`](../../src/grammar/schemas/).

The grammar validator and spec compiler live in [`src/grammar/`](../../src/grammar/) and
are exported from the package root (`validateSpec`, `compileSpec`).
